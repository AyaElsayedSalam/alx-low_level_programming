#include "main.h"
/**
* mul - a function that multiplies two integers.
 * Return: 1 if c is uppercase Returns 0 otherwise
 * @a: the first integer input parameter
 * @b: the second integer input parameter
*/
int mul(int a, int b)
{
return (a * b);
}
